import sys
import csv

# Given a csv file, returns a dictionary with keys that are all the top songs collected by Spotify, whose values are a tokenized list of its lyrics.
def tokenize(filename):

    return